#!/bin/sh
# prerm — stop the service before remove/upgrade.
set -e

UNIT=voipstack-sip-sequencer.service

case "$1" in
  remove|upgrade|deconfigure)
    if [ -d /run/systemd/system ]; then
      systemctl stop "$UNIT" >/dev/null 2>&1 || true
    fi
    ;;
esac

exit 0
